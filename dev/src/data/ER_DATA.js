import COLUMN_INSTANCES from './COLUMN_INSTANCES';
import COLUMNS          from './COLUMNS';
import ENTITIES         from './ENTITIES';
import PORTS            from './PORTS';
import RELASHONSHIPS    from './RELASHONSHIPS';

const ER_DATA = {
    TABLES:           ENTITIES,
    COLUMNS:          COLUMNS,
    COLUMN_INSTANCES: COLUMN_INSTANCES,
    RELASHONSHIPS:    RELASHONSHIPS,
    PORTS:            PORTS,
};

export default ER_DATA;
